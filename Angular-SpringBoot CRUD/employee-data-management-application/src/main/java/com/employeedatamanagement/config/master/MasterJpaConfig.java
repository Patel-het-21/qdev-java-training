package com.employeedatamanagement.config.master;

import org.springframework.context.annotation.Configuration;

/**
 * JPA configuration for the master database.
 * Configures the master data source, entity manager, and transaction manager.
 */
@Configuration
public class MasterJpaConfig {

    // Configuration for master database
    // This would typically configure the master DataSource and EntityManagerFactory
}